# environment_monitoring_system
A Python based IoT simulator that shows temperature, humidity and air quality data via MQTT (Public Broker) and publishes it.  The readings are stored in SQLite, MongoDB Atlas and Neo4j Aura demonstrating real time data.
